# Quant1X 多语言量化框架架构与开发规范

## 1. 架构概述

Quant1X 是一个面向多技术栈开发者的量化交易基础设施框架。本框架采用纯原生、平行实现的设计理念，支持 C/C++、Rust、Python 和 Go 四种语言。

### 核心设计哲学

- **零胶水/无跨语言依赖**：各语言独立编译、独立运行，严禁使用 Cgo 或 FFI 进行跨语言调用。
- **极致语义对齐**：通过物理结构的对称性和高度一致的函数签名，抹平语言差异，降低跨语言阅读与二次开发的心智负担。
- **数据契约优先**：在 I/O 边界强制统一数据格式，确保异构系统间的数据无缝流转。
- **AI 友好架构**：通过高一致性的代码结构和标准化的文档，为 AI 辅助编程和多语言自动扩展提供完美的上下文基座。

## 2. 目录与文件结构规范

模块按功能划分目录结构，相同功能处于同一级别的目录。为了在保持语义一致的同时解决 Go 语言严格的循环依赖问题，采用 **"父目录平铺多语言源文件 + Go 专属子目录"** 的物理结构。

### 2.1 标准目录树示例

以 data 模块下的 t1 功能为例：
```text
data/
├── t1/
│   ├── api.go
│   └── impl.go
├── t1.py
├── t1.h
├── t1.cpp
└── t1.rs

```

### 2.2 Go 语言的特殊处理原则

- **规避循环依赖**：Go 的包（Package）边界是目录。将 Go 实现下沉到 `t1/` 子目录，利用 Go 的包级别可见性进行物理隔离，从根本上规避 Go 编译期的循环依赖检查。
- **语义对齐**：外部调用 Go 代码时，路径为 `import "proj/data/t1"`，调用方式为 `t1.Func()`，与 Python (`t1.func()`)、Rust (`t1::func()`) 保持命名空间前缀的绝对一致。
- **包名铁律**：`t1/` 目录下的所有 `.go` 文件，首行必须严格声明为 `package t1`。

## 3. 命名与签名规范

在内存态（代码编写）中，尊重各语言的原生社区规范；在边界态（I/O 序列化）中，强制统一契约。

### 3.1 代码内命名规范（内存态）

| 语言   | 类/结构体/类型名       | 函数/方法签名          | 变量/字段名            | 备注                         |
|--------|------------------------|------------------------|------------------------|------------------------------|
| Python | PascalCase             | snake_case             | snake_case             | 遵循 PEP 8                   |
| C/C++  | PascalCase             | snake_case             | snake_case             | 保持与 Python 视觉一致       |
| Rust   | PascalCase             | snake_case             | snake_case             | 遵循 Rust API Guidelines     |
| Go     | PascalCase (导出) / camelCase (非导出) | PascalCase (导出) / camelCase (非导出) | PascalCase (导出) / camelCase (非导出) | **必须遵循 Go 官方规范，严禁在 Go 中使用蛇形命名** |

### 3.2 序列化与 I/O 命名规范（边界态）

**【铁律】所有落地数据（CSV, JSON, Parquet, Protobuf 等）的字段名，强制全小写蛇形（snake_case）。**

在 Go 语言中，必须通过 Struct Tag 将内存中的驼峰变量映射为小写蛇形字段，以确保跨语言读写完全一致。

Go 代码示例：
```go
type Bar struct {
    Open  float64 `json:"open" csv:"open"`
    High  float64 `json:"high" csv:"high"`
    Low   float64 `json:"low" csv:"low"`
    Close float64 `json:"close" csv:"close"`
}
```

## 4. 文档与 AI 协同规范

重要模块必须配备同级 `.md` 文档。这些文档不仅是人类阅读的说明，更是 AI 编程工具进行跨语言代码生成的 **"真理之源（Source of Truth）"**。

### 4.1 标准文档模板 (t1.md)

每个重要模块的 `.md` 文档应包含以下核心 Block：

- **业务定义**：一句话说明该模块在量化链路中的作用。
- **数学模型 / 核心公式**：使用 LaTeX 语法写出核心公式（AI 对 LaTeX 的代码转换准确率极高）。
- **数据契约 (Schema)**：明确输入/输出的字段、类型（特别是浮点数精度要求）及是否允许 Null/NaN。
- **边界与异常处理**：明确定义除零、空数组、非交易时间戳等极端情况的处理逻辑。

### 4.2 AI 辅助开发规则 (Prompt 约束)

在使用 AI 工具（如 Cursor, Copilot）生成 Rust/Go 代码时，需遵循以下系统级提示：

- **上下文绑定**：生成代码前，必须读取同级目录的 `.md` 文档及 Python/C++ 参考实现。
- **命名转换**：自动将 Python/C++ 的 `snake_case` 函数名转换为 Go 的 `PascalCase`。
- **Tag 对齐**：生成 Go 结构体时，必须自动补全 `json`/`csv` tag，且 tag 内容必须为全小写蛇形。
- **边界覆盖**：生成的代码必须严格覆盖 `.md` 文档中定义的所有【边界与异常处理】逻辑。

## 5. 标准开发工作流

新功能块的开发遵循 **"验证 -> 生产 -> 扩展"** 的渐进式流程：

1. **Python 先行 (探索与验证)**
   - 研究员使用 Python 快速实现逻辑，验证数学模型和业务可行性。
   - 产出：`t1.py` 及配套的黄金测试数据。

2. **C++ 实现 (生产与极致性能)**
   - 核心开发将 Python 逻辑转化为 C++ 生产代码，优化内存与计算性能。
   - 产出：`t1.h`, `t1.cpp`。此时需同步完善 `t1.md` 文档。

3. **Rust / Go 跟进 (AI 辅助扩展)**
   - 利用 AI 工具，以 `t1.md` 为上下文，参考 `t1.py` 和 `t1.cpp` 的签名与逻辑，自动生成 Rust 和 Go 的平行实现。
   - 产出：`t1.rs`, `data/t1/impl.go`。

4. **跨语言一致性校验 (CI/CD)**
   - 将统一的黄金测试数据喂给四种语言的实现。
   - 断言四种语言的输出结果在设定的精度（epsilon）内严格相等，防止逻辑分叉。

## 6. 核心设计原则总结

- **物理隔离，语义同构**：利用 Go 的子目录机制解决语言特性冲突，对外暴露统一的命名空间。
- **内方外圆，数据互通**：内存代码顺应语言天性，I/O 边界死守全小写蛇形契约。
- **文档即代码，AI 即生产力**：将业务逻辑固化为结构化文档，让 AI 承担繁琐的跨语言语法翻译工作，确保多语言实现的绝对一致。

## 7. 版本与发布规则

- 项目版本号由 Git Tag（`vMAJOR.MINOR.PATCH`）唯一决定。
- 所有语言（Rust / Python / Go / C++）必须使用同一 Tag 作为版本号，禁止各语言独立维护版本。
- `autochangelog` 是唯一合法的版本发布入口，所有版本发布必须通过 `autochangelog` 触发。
- AI Agent 禁止自行生成、推断或修改版本号。版本号仅由维护者通过 Git Tag 手动设定。
